foreach(example PipeRedisExample QpsPoolExample LatchExample GoPipeExample)
    add_executable(${example} ${example}.cc)
    target_link_libraries(${example} workflow workflow-extra)
    add_dependencies(${example} workflow-extra)
endforeach()
